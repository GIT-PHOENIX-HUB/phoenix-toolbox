{
  "name": "phoenix-365",
  "version": "0.1.0",
  "private": true,
  "description": "Phoenix 365 — Microsoft Graph MCP server and shared utilities for Phoenix Electric AI",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "npm run build && node packages/mcp-server/dist/index.js"
  }
}
