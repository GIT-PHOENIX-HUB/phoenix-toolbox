---
name: example
description: Template command — replace with your command description
---

# /example

This is a template command file. Replace all content with your actual command.

## Usage

```
/example [arguments]
```

## What It Does

1. Describe step 1
2. 2. Describe step 2
   3. 3. Describe step 3
     
      4. ## Template Note
     
      5. > Commands use YAML frontmatter for metadata. The `name` and `description` fields are required.
         > > See `capabilities/echo-persistence/commands/` for working examples.
